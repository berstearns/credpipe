# your private deployment — never publish
.env

# per-run runbooks (contain the real relay IP / deployment specifics)
runbook/

# never let a key escape into git, whatever it's named
*.key
key
creds.enc
*.enc
